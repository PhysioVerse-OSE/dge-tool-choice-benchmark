# Data Availability and Input Requirements

Raw RNA-seq count matrices and large processed datasets are not stored in this GitHub repository.

Users should obtain datasets from their original public repositories or archives, then place them locally in a folder that is excluded from Git tracking, such as:

data/raw/

The repository includes analysis code and figure-generation workflows, but it does not redistribute raw data.
